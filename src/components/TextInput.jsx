const TextInput = () => {
  return (
    <textarea
      name="description"
      id="description"
      cols="30"
      rows="10"
      defaultValue="Type here..."
      placeholder="Type here..."
    ></textarea>
  );
};
export default TextInput;
